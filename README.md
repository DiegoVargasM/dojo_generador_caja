This project was bootstrapped with Create React App.

It consists of an input to add a colored box to a display component using a form component.

It was made using useState to update an array to store the color values and lift its state up to show the boxes in the display component.
